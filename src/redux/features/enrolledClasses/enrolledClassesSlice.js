/* eslint-disable no-empty-pattern */
import { createSlice } from "@reduxjs/toolkit";

const initialState = {};
const enrolledClassesSlice = createSlice({
  name: "users",
  initialState,
  reducers: {},
});
export const {} = enrolledClassesSlice.actions;
export default enrolledClassesSlice.reducer;
